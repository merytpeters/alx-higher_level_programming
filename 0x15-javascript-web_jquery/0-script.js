document.addEventListener('DOMContentLoaded', () => {
  // Selcet the header element
  const headerElement = document.querySelector('header');

  // Update the text color of the header element
  if (headerElement) {
    headerElement.style.color = '#FF0000';
  }
});
